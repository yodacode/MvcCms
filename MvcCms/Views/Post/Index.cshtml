@model IEnumerable<MvcCms.Models.Tag>
@{
    ViewBag.Title = "Store";
}
<h3>Browse Genres</h3>

<p>
    Select from @Model.Count() genres:
</p>

<ul>
    @foreach (var tag in Model)
    {
        <li>@Html.ActionLink(tag.Name, "Browse", new { tag = tag.Name })</li>
    }
</ul>